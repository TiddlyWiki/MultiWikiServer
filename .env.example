# Example environment variables for Docker Compose
# Copy this file to .env and adjust values as needed

# Port to expose MWS on the host
MWS_PORT=8080

# Timezone for the container (optional)
TZ=UTC

# Node environment
NODE_ENV=production

# Database URL (for SQLite, this is the default)
DATABASE_URL=file:./store/mws.db
